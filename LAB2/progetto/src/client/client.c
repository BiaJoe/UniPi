#include "client.h"

int main(int argc, char* argv[]){

	// elabora argc e argv 	
	if(argc != 3 || argc != 5)
		return 0;

	// distingui tra modalità file e modalità input diretto

	// chiama una funzione per gestire la chiamata posix
	// nel caso input diretto fine, nel caso del file usa una funzione
	// che itera con getline e scorre todos

	// il server gestirà la logica delle emergenze

}